namespace iRadar.App;

internal static class Program
{
    [STAThread]
    private static int Main(string[] args)
    {
        Console.WriteLine("iRadar — Fase 0 placeholder. Composition root will land in Fase 4.");
        return 0;
    }
}
