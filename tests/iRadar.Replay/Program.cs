namespace iRadar.Replay;

internal static class Program
{
    private static int Main(string[] args)
    {
        Console.WriteLine("iRadar.Replay — Fase 0 placeholder. IBT playback lands in Fase 3.");
        return 0;
    }
}
